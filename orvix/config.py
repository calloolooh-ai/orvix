"""
config.py

loads and saves orvix's settings: calibration box, pinch/grab thresholds,
filter params, target fps, which hand to track. lives in orvix_config.yaml
at the repo root by default, gitignored once it has your personal
calibration data in it (see .gitignore) so we ship a template instead.

not implemented yet, this is a scaffold stub.
"""

# TODO: dataclass for settings
# TODO: load_config(path) / save_config(settings, path)
# TODO: sane defaults so orvix runs before you've ever calibrated
